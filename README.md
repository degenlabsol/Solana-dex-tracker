# Solana DEX Tracker Bot

A Node.js bot that monitors DEX Screener for boosted Solana tokens and sends alerts to Telegram.

## Setup

1. Install dependencies:
   npm install

2. Configure:
   Rename .env.example to .env and fill in values.

3. Start:
   npm start
